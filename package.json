{
  "name": "restaurant-menu-delta-monitor",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Apify Actor that detects restaurant menu and price changes between scheduled runs.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test test/*.test.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "dependencies": {
    "apify": "^3.4.0",
    "cheerio": "^1.1.2",
    "got-scraping": "^4.2.1",
    "playwright": "1.58.1"
  }
}
